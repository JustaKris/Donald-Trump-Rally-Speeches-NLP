###############################################################################
# .env.example - Environment Configuration Template
#
# Copy this file to `.env` and customize for your deployment.
# Keep your `.env` file private - DO NOT commit it to version control!
#
# CONFIGURATION STRATEGY:
#   - YAML configs (configs/development.yaml, configs/production.yaml):
#     General defaults, model names, non-sensitive settings
#   - THIS FILE (.env):
#     Secrets (API keys), deployment-specific overrides
#
# Precedence: Code defaults → YAML → .env → Environment variables
###############################################################################

# ============================================================================
# Environment Selection
# ============================================================================
# Which YAML config to load: development, production, staging
ENVIRONMENT=development

# ============================================================================
# LLM Provider (SENSITIVE - Required for AI features)
# ============================================================================
# ** API KEY - KEEP SECRET **
# Get keys from:
#   - Gemini: https://ai.google.dev/
#   - OpenAI: https://platform.openai.com/api-keys
#   - Anthropic: https://console.anthropic.com/
LLM_API_KEY=your-api-key-here

# Optional LLM overrides (defaults set in YAML)
# LLM_PROVIDER=gemini
# LLM_ENABLED=true
# LLM_MODEL_NAME=gemini-2.5-flash
# LLM_TEMPERATURE=0.3
# LLM_MAX_OUTPUT_TOKENS=1024

# ============================================================================
# API Configuration (DEPLOYMENT-SPECIFIC)
# ============================================================================
# Override these for your deployment environment
# API_HOST=0.0.0.0
# API_PORT=8000
# API_RELOAD=true  # Set true for development hot-reload
# API_CORS_ORIGINS=["*"]  # Use ["https://yourdomain.com"] in production

# ============================================================================
# Optional Configuration Overrides
# ============================================================================
# Uncomment any of these to override YAML defaults

# --- Logging ---
# LOG_LEVEL=DEBUG

# --- ML Models ---
# MODEL_SENTIMENT_MODEL_NAME=ProsusAI/finbert
# MODEL_EMOTION_MODEL_NAME=j-hartmann/emotion-english-distilroberta-base
# MODEL_EMBEDDING_MODEL_NAME=all-mpnet-base-v2
# MODEL_RERANKER_MODEL_NAME=cross-encoder/ms-marco-MiniLM-L-6-v2

# --- Data Paths ---
# PATH_SPEECHES_DIRECTORY=./data/Donald Trump Rally Speeches
# PATH_DATA_ROOT_DIRECTORY=./data

# --- RAG Search Configuration ---
# RAG_CHROMADB_PERSIST_DIRECTORY=./data/chromadb
# RAG_CHROMADB_COLLECTION_NAME=speeches
# RAG_CHUNK_SIZE=2048
# RAG_CHUNK_OVERLAP=150
# RAG_DEFAULT_TOP_K=5
# RAG_USE_RERANKING=true
# RAG_USE_HYBRID_SEARCH=true

# ============================================================================
# Quick Start Examples
# ============================================================================

# Google Gemini (Free tier available):
#   LLM_PROVIDER=gemini
#   LLM_API_KEY=AIza...
#   LLM_MODEL_NAME=gemini-2.5-flash

# OpenAI GPT:
#   LLM_PROVIDER=openai
#   LLM_API_KEY=sk-proj-...
#   LLM_MODEL_NAME=gpt-4o-mini

# Anthropic Claude:
#   LLM_PROVIDER=anthropic
#   LLM_API_KEY=sk-ant-...
#   LLM_MODEL_NAME=claude-3-5-sonnet-20241022

# Disable LLM (extraction-based answers only):
#   LLM_PROVIDER=none
#   # or set LLM_ENABLED=false


